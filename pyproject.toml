[project]
name = "b2-file-manager"
version = "0.1.0"
description = "B2 网页版上传/下载小工具"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.28.0",
    "flask>=3.0",
    "gunicorn>=23.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
